import styled from "styled-components";
import { flex, flexbox, space, typography, layout } from "styled-system";

export const Flex = styled.div`
  display: flex;
  ${flexbox}
  ${space}
  ${typography}
  ${layout}
`;
